<% form_for @state do |f| %>
  <%= f.error_messages %>
  <%= avatar(current_user,"50_50")%>
  <p>
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>
